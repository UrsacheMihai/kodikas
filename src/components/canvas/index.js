/**
 * Canvas Components Barrel Export
 * Exports canvas visualizers and background components.
 * 
 * Optional:
 * - Export additional graphical canvas layers (e.g. particle overlay, radar scan).
 */

export { default as KeynoteBackground } from './KeynoteBackground.jsx'
export { default } from './KeynoteBackground.jsx'
