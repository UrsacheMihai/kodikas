/**
 * Custom Hooks Barrel Export
 * Centralizes and exports custom React hooks used across the application.
 * 
 * Optional:
 * - Export new utility hooks (e.g., useWindowSize, useScrollProgress, useGamepad) as features expand.
 */

export { useKeynoteController, default } from './useKeynoteController.js'
export { ThemeProvider, useTheme } from './useTheme.js'
